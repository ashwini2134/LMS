# Hints

- Use `input()` to get text from the user.
- Store the user's text in a variable.
- Python strings have built-in methods like `.lower()`.
- Calling `.lower()` on a string converts all letters to lowercase.
- Print the modified string using `print()`.