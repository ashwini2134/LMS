# Hints

- Use `input()` to get text from the user.
- Store the text in a variable.
- Python strings have built-in methods.
- Use the `.lower()` method to convert text to lowercase.
- Print the updated text using `print()`.