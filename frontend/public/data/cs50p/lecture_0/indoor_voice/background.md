# Background

Strings are used in Python to store text. Python provides many built-in string methods that allow programs to modify and process text easily.

One useful string method is `.lower()`. This method converts all uppercase letters in a string into lowercase letters. It does not affect numbers, spaces, or symbols.

Programs often use text conversion methods to standardize user input and make text easier to process or compare.