# Making Faces

Implement a function called convert that accepts a str as input and returns that same input with :) converted to the emoji and :( converted to the emoji. Then, in the same file, implement a function called main that prompts the user for a string of text, calls convert on that input, and prints the result.

## Task

> TODO: Restate the task clearly for the student.
