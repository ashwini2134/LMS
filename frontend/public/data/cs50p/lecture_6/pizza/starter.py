# pizza.py
# Read the CSV file named by the single CLI argument and print it as a
# tabulate "grid" table. Exit via sys.exit on misuse.
# Uses the third-party `tabulate` module (pip install tabulate).

# TODO: implement
