# figlet.py
# Output text in a FIGlet font: random with zero CLI args, or a specific font
# with `-f FONT` / `--font FONT`. Exit via sys.exit("Invalid usage") on misuse.
# Uses the third-party `pyfiglet` module (pip install pyfiglet).

# TODO: implement
